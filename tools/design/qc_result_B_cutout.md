用户希望我作为游戏美术素材视觉质检员，对一张抠图后的透明背景PNG立绘进行质量检查。
角色描述：亚洲青年男性，深灰蓝T恤全身立绘。

需要检查的5项：
1. 主体轮廓是否干净，有无毛边/锯齿/碎屑；
2. 有无残缺（缺部件、断肢、边缘被削掉）；
3. 有无因抠图造成的镂空或窟窿（尤其两臂与躯干之间、双腿之间、裤子/大腿处、发丝之间）；
4. 透明区是否有残留脏点/杂色像素/半透明残留；
5. 主体与背景分离是否自然，边缘有无白边/黑边/光晕残留。

输出格式：
第一行【verdict】= 合格 或 需微调(列具体问题) 或 需重生成(列具体问题)。
随后对以上5项逐条给观察与结论。
最后给出总体理由并列出可执行问题清单（包含问题所在部位与程度）。

观察图片：
1. 轮廓：头发边缘、手臂边缘、腿部边缘看起来比较干净，但大腿处有明显的白色块。
2. 残缺：大腿中间（右腿大腿前侧，即画面左侧那条腿的大腿部分，或者说是两腿之间偏左的位置）有一大块白色的缺失，看起来像是被削掉了或者抠图抠漏了，露出了背景色（或者是原图背景色没抠干净，或者是抠图把主体抠掉了）。仔细看，那是大腿前侧的一块白色区域，形状不规则，明显是主体缺失（窟窿/残缺）。
3. 镂空/窟窿：大腿处（画面左侧腿的大腿前侧，靠近大腿根部/膝盖上方）有一大块明显的白色/透明缺失，看起来像是把裤子或皮肤抠没了。这属于严重的镂空/残缺。两臂与躯干之间看起来还好，没有明显的背景透过来。双腿之间看起来正常。
4. 透明区残留：背景看起来比较干净，是纯白/透明。但在人物边缘，特别是深色衣服边缘，可能有一些抗锯齿的半透明残留，或者深色像素残留（黑边/光晕）。看衣服边缘，有一些深色的溢出。
5. 分离自然度：边缘看起来有些生硬，特别是深色衣服和深色裤子与背景的对比。

详细分析第3点（镂空/窟窿/残缺）：
在角色右腿（画面左侧）的大腿前侧，有一块明显的白色/透明区域。这看起来像是原图里的高光或者背景，被错误地识别为透明背景，或者在抠图过程中被删除了。这导致裤子/大腿看起来有个大洞。这是一个严重的缺陷。
另外，看大腿中间，两块白色区域。
等等，仔细看图片，大腿中间（两腿之间偏上）有一块白色的东西。
那是右腿（画面左侧）的大腿前侧，有一块白色的不规则形状。这明显是抠图失误，把主体的一部分（可能是裤子的高光或者皮肤）当成背景抠掉了，或者原图这里就是白的？
不，看整体，这是一张全身立绘。大腿处（画面左侧腿，即角色的右腿）大腿前侧有一大块白色的区域，看起来像是被“吃”掉了。这属于严重的残缺/镂空。
再看，两块白色区域。一块在右腿大腿前侧（画面左侧），一块在左腿大腿前侧（画面右侧，稍微小一点）。
这看起来像是原图里的高光部分被误抠了，或者是AI生成/处理时的瑕疵。
这属于“因抠图造成的镂空或窟窿”以及“残缺”。

让我们重新审视图片。
角色穿着深灰蓝T恤和深色裤子。
在腰带下方，右腿（画面左侧）大腿前侧，有一块明显的白色/透明区域。形状像是一个倒三角或不规则块。
在左腿（画面右侧）大腿前侧，也有一小块白色区域。
这明显是抠图把主体的一部分抠掉了，或者原图这里就是透明的（如果是AI生成的，可能这里没生成好）。
如果是抠图问题，那就是严重的镂空/残缺。

再看边缘：
头发边缘：比较干净，但有一些细碎的发丝可能没抠好，或者有点黑边。
手臂边缘：轮廓比较清晰。
衣服边缘：深蓝色衣服，边缘有一些深色的像素残留（黑边/光晕），这是常见的抠图边缘溢出问题。

透明区：
背景看起来是白色的（在预览中），如果是透明PNG，这里显示为白色。
仔细看背景，非常干净，没有明显的脏点。

总结问题：
1. 轮廓：基本干净，但深色物体边缘可能有轻微黑边/光晕。
2. 残缺：大腿处（特别是画面左侧腿的大腿前侧）有明显的白色/透明缺失块，像是被削掉了。
3. 镂空：大腿处有大块镂空（白色块）。
4. 透明区：相对干净。
5. 分离：边缘有轻微黑边/光晕残留（深色衣服边缘）。

修正观察：
看大腿那个白色的块。那是裤子的高光吗？不，那是纯白/透明。如果是透明，那就是抠图把裤子的高光或者皮肤抠掉了。如果是白色背景，那就是原图背景。
用户说是“透明背景PNG”。如果预览显示白色，可能是查看器背景是白色。
那个白色的块明显是主体缺失。
位置：角色右腿（画面左侧）大腿前侧，以及左腿（画面右侧）大腿前侧偏上。
这看起来非常像AI生成图像时的瑕疵，或者抠图算法把高亮部分当成了背景。
这属于严重的“残缺”和“镂空”。

再看发丝：发丝边缘比较实，没有太好的半透明过渡，可能有点生硬。

让我们逐条写：
1) 主体轮廓：整体轮廓基本清晰，但在深色衣物（T恤、裤子、靴子）与背景交界处，存在轻微的黑色/深色像素残留（光晕/黑边），发丝边缘略显生硬，缺乏自然的半透明过渡。
2) 有无残缺：有。角色大腿部位（特别是画面左侧腿的大腿前侧，以及右侧腿大腿前侧上方）存在明显的块状缺失（显示为白色/透明），疑似原图高光或细节被误删，导致衣物/腿部外观不完整。
3) 有无镂空：有。大腿处（画面左腿大腿前侧）有明显的非主体镂空（白色/透明块），破坏了腿部的完整性。两臂与躯干之间、双腿之间基本正常，但大腿处的窟窿非常严重。
4) 透明区残留：透明区（背景）基本干净，无明显脏点。但在主体深色边缘周围，可能有极细微的半透明深色残留（抗锯齿问题）。
5) 分离自然度：深色主体（深蓝/黑）与透明背景分离时，边缘出现了明显的黑边/光晕残留（Color Fringing），不够自然。

Verdict: 需微调(大腿处严重镂空/残缺，深色边缘黑边/光晕残留，需修复主体缺失并清理边缘)。

再仔细看图，大腿那个白色的块。
那是右腿（画面左侧）的大腿。
那是左腿（画面右侧）的大腿上方。
这看起来像是原图里的高光被抠掉了。
这绝对是“需重生成”或者“需大幅微调”。
如果是抠图问题，把高光抠没了，那得重画或者重抠。
如果是AI生成的图，那得重生成。
用户说是“抠图后的...成品”。说明原图是有的，只是抠图抠坏了。
那这就是抠图质量差。

问题清单：
1. 大腿处（画面左腿大腿前侧、右腿大腿前侧上方）：严重镂空/残缺，大块主体像素丢失，需重绘或重新抠图修复。
2. 深色衣物/靴子边缘：黑边/光晕残留，需清理边缘羽化或重抠。
3. 头发边缘：略显生硬，缺乏发丝透明度，需微调边缘。

等等，再看一眼图。
那个白色的块，形状很奇怪。
在腰带下方，左腿（画面右侧）的大腿上方，有一块白的。
在右腿（画面左侧）的大腿中部，有一块大的白的。
这看起来像是原图里就是白的？
不，如果是深灰蓝T恤和深色裤子，这里不应该是白的。
这肯定是抠图把高光抠没了，或者原图这里有问题。
如果是AI生成的图（看风格很像AI），AI经常在大腿、关节处出错。
看脸，脸有点AI感（眼神、皮肤质感）。
看手，左手（画面右侧）握拳，手指有点模糊。右手（画面左侧）拿枪？
右手（画面左侧）拿着一个黑色的物体，像是枪套或者枪。
腿上的绑带（腿挂枪套）也很像AI生成的，结构有点乱。
这很可能是一张AI生成的图，然后做了简单的抠图。
如果是这样，大腿处的白色块可能是AI生成时的错误（比如背景没生成好，或者高光错误），然后抠图软件把白色当成了背景抠掉了（如果原图背景是白色的话）。
或者抠图软件（如Photoshop快速选择）把白色高光区域当成了背景。

无论如何，作为质检员：
Verdict: 需重生成(大腿处存在严重主体缺失/镂空，AI生成瑕疵明显；深色边缘存在严重黑边/光晕)。
或者 需微调(如果认为只是抠图问题)。
通常这种明显的主体缺失（大腿缺块），如果是抠图造成的，必须重做抠图或修复。如果是原图（AI生成）就有的，必须重生成。
看用户的描述：“这是一张抠图后的透明背景PNG立绘成品”。
暗示原图是好的，只是抠图。
但看大腿那个白块，太像AI生成的瑕疵被抠图放大了，或者原图就是AI生成的且有问题。
如果是抠图把高光抠没了，那是抠图算法问题（比如基于颜色的抠图，把白色高光当背景了）。
如果是这样，结论是“需重抠图/修复”。

让我们定调为：需重生成(或需大幅修复)。
因为大腿处的缺失太明显了，而且边缘黑边也很明显。
通常游戏美术对这种立绘要求很高。
大腿缺块是致命伤。

逐项评估：
1) 主体轮廓：轮廓基本完整，但边缘（特别是深色衣物）有锯齿和毛边，发丝处理生硬。
2) 残缺：大腿部位（画面左侧腿大腿前侧、右侧腿大腿上方）有明显的大块缺失，像是被削掉了。
3) 镂空：大腿处有大面积镂空（白色/透明区域），破坏了角色完整性。
4) 透明区：背景较干净，但主体周围有深色残留。
5) 分离：深色主体与背景分离差，有明显黑边/光晕。

最终Verdict：需重生成(大腿处严重主体缺失/镂空，深色边缘黑边/光晕严重，整体抠图质量差/原图可能有AI瑕疵)。
或者写：需微调(大腿处镂空需修复，边缘黑边需清理)。
考虑到“需重生成”通常用于原图完全不行。这里原图（如果有的话）可能只是抠图没做好。
但看大腿那个白块，如果是抠图把高光抠了，那得重新抠（用蒙版）。
如果是AI图本身大腿就是白的（生成错误），那得重生成。
看风格，这非常像Midjourney或Stable Diffusion生成的图（光影、肌肉线条、衣服褶皱、枪套结构）。AI图经常在大腿、手部出错。
大腿那个白块很可能是AI生成时的“穿帮”或者高光错误，然后被抠图软件（如果原图背景是白的）当成了背景抠掉了。
或者抠图软件（如Remove.bg）把白色高光当成了透明背景。
结论：这图质量不行。

我会给出“需重生成”或“需大幅修复”。
根据提示词格式：【verdict】= 合格 或 需微调(列具体问题) 或 需重生成(列具体问题)。
我会选 **需重生成(大腿处严重主体缺失/镂空，深色边缘黑边/光晕残留，疑似AI生成瑕疵被误抠)**。
或者 **需微调(大腿处有严重镂空/残缺需修复，深色衣物边缘有黑边/光晕需清理，发丝边缘生硬)**。
通常如果是抠图员的工作，他会说“需重抠/修复”。
如果是美术验收，会打回。
这里我作为质检员，指出问题。

让我们仔细看大腿。
画面左侧腿（角色右腿），大腿前侧，有一大块白色。
画面右侧腿（角色左腿），大腿前侧上方，有一小块白色。
这绝对是主体缺失。

再看边缘。
深蓝色T恤边缘，有一圈深黑色/深蓝色的像素，这是典型的抠图边缘溢出（Backscatter/Color Fringing）。
靴子边缘也有。

所以：
1. 轮廓：有毛边（深色边缘溢出）。
2. 残缺：大腿处缺块。
3. 镂空：大腿处有窟窿。
4. 透明区：相对干净。
5. 分离：有黑边。

Verdict: 需重生成(大腿处存在严重主体缺失与镂空，深色边缘存在明显黑边/光晕残留，整体抠图质量不达标，建议重新进行精细抠图或修复原图)。
或者简单点：需微调(大腿处严重镂空/残缺，边缘黑边)。
不，大腿缺块这么严重，肯定是“需重生成”或者“需大幅修复”。
提示词里只有“合格”、“需微调”、“需