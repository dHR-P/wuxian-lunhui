===== pc_wan4 :: OK
content: 基于您提供的角色设定（健康亚洲青年男性战士/郑吒，深灰蓝T恤+战术裤，正常站姿握拳）及前置像素线索，以下是对【原始立绘 RAW】与【抠图切割图 CUTOUT】的逐项核验报告：

### ⚠️ 前置线索核实
**核实结果**：**符合线索描述**。RAW图底部两下角确实偏亮（可见明显的灰蓝色地面质感，Luminance值较高），存在明显的地面反光/泛光/渐变现象，背景并非绝对纯黑。

---

### 📷 【原始立
reasoning_content: 用户要求我作为资深游戏美术质检员，对提供的两张图片（原始RAW和抠图CUTOUT）进行逐项核验。
设定角色：健康亚洲青年男性战士（主角郑吒），深灰蓝紧身T恤+深色战术裤，正常人类战士站姿握拳，非丧尸非变异。
前置像素线索：raw底部两下角偏亮（lum≈51-55, 灰蓝色调），疑似底部地面反光/泛光。

需要核验的7点：
1. 全身完整无裁剪（头顶/双手/脚完整；脚可被画面底缘轻微裁切）
2. 背景是否绝对平面纯黑，无反射/投影/渐变/光晕
3. 脚掌贴近画面底缘，下方留白小
4. 轮廓上是否有白色描边/白边残留/白或浅蓝边缘光晕
5. (pc_wan4/guard/zombie)双手可见且手指清晰分离；(hunter) 爪/刀清晰分离、不糊不融合
6. 下半身(腿/脚/下躯干)是否明亮清晰、不融进黑背景(hunter 尤其无黑剪影)
7. cutout: 背景全透明、主体完整无镂空大洞、无白边/黑边残留、无碎点杂质、轮廓干净平滑

图片1：原始立绘 RAW
- 角色：亚洲男性，深灰蓝T恤，深色战术裤，站立握拳。符合设定。
- 1. 全身完整：头顶有空间，双手可见，脚部（鞋子）完整，贴近底边。通过。
- 2. 背景纯黑：背景不是绝对纯黑，底部有明显的灰蓝色地面反光/渐变（符合前置线索）。不通过。
- 3. 脚掌贴近底缘：鞋子底部离底边还有一段距离，下方留白较大（有地面反光区域）。不通过（或者说留白不小，因为有地面）。
- 4. 轮廓白边：无明显白边。
- 5. 双手：双手握拳，手指清晰（虽然握拳，但手型正常）。通过。
- 6. 下半身：明亮清晰，鞋子有细节，没有融进黑背景（因为底部有地面反光）。通过。
- 7. cutout判定：这是RAW图，不适用第7点，或者看第二张图。

图片2：抠图切割图 CUTOUT
- 1. 全身完整：同RAW。通过。
- 2. 背景纯黑：背景是黑色，但底部有大片白色区域（抠图错误，把地面反光抠成了白色或者没抠干净，留下了白色杂边）。不通过。
- 3. 脚掌贴近底缘：同RAW。
- 4. 轮廓白边：底部有非常明显的白色残留/白边，甚至大面积白色块。不通过。
- 5. 双手：同RAW。通过。
- 6. 下半身：同RAW。通过。
- 7. cutout判定：背景不是全透明（看起来像是黑色背景上加了白色块，或者是Alpha通道搞错了，显示为白色）。主体底部有大片白色残留（应该是抠图把地面反光当成前景保留了，或者背景没抠干净变成了白色）。轮廓极不干净。不通过。

重新仔细看图2：图2看起来像是在图1的基础上，底部加了白色的遮罩或者抠图时把底部地面反光亮部抠出来了，变成了白色锯齿状边缘。这明显是抠图失败。背景应该是透明（如果是PNG）或者纯黑。这里显示的是黑色背景+底部白色大块。这说明抠图Alpha通道有问题，或者显示模式有问题。如果是CUTOUT，通常背景是透明（棋盘格）或纯黑。这里底部有大片白色，说明抠图边缘处理极差，把地面的高光全抠出来了，或者反选错了。

让我们重新对应7点要求。
要求是分别对【原始立绘 RAW】与【抠图切割图 CUTOUT】各逐条判定。

**RAW图分析：**
1. 全身完整：是。头顶、双手、双脚（鞋子）都在画面内。
2. 背景绝对平面纯黑：否。底部有明显的灰蓝色地面反光/渐变（符合前置线索 lum≈51-55）。背景有渐变，从顶部的纯黑到底部的深灰蓝。
3. 脚掌贴近底缘，下方留白小：否。脚下方有明显的地面反光区域，留白（其实是地面）较大。
4. 轮廓白边：无明显白边。
5. 双手：双手握拳，可见，正常人类手型。
6. 下半身：清晰，不融合。
7. 不适用（这是RAW）。

**CUTOUT图分析：**
图2看起来背景还是黑的，但底部有一大条白色的东西。这看起来像是抠图时的“白边残留”或者“抠图溢出”。
1. 全身完整：是。
2. 背景绝对平面纯黑：否。底部有大面积白色/亮色块。
3. 脚掌贴近底缘：否。
4. 轮廓白边：底部有严重的白色残留/白边（锯齿状白色区域）。
5. 双手：正常。
6. 下半身：正常。
7. cutout判定：背景不全透明（如果是PNG，这里显示黑色背景+白色块，说明Alpha通道把底部地面抠成了白色前景，或者背景没去干净且显示为白）。主体底部有大洞/错误区域（白色锯齿）。轮廓极不干净，底部有大片白色杂边。

修正建议：
RAW图：底部有地面反光，如果设定需要绝对黑背景，需要去除底部地面反光，或者裁切掉底部。
CUTOUT图：抠图完全失败。底部把地面反光全部抠出来了，变成了白色锯齿。需要重新抠图，使用“选择主体”或调整阈值，把底部地面（包括反光）全部去掉，只保留人物。或者如果保留地面反光，那背景就不是纯黑了。根据设定“背景是否绝对平面纯黑”，RAW图本身就不符合“绝对平面纯黑”（有地面反光）。

等等，题目说“背景是否绝对平面纯黑...请重点核实: 底部是否有地面反光/泛光/渐变, 背景是否绝对平面纯黑。”
这说明RAW图本身可能就不符合“背景绝对纯黑”的要求，这是一个质检点。

让我们逐项写判定。

**【原始立绘 RAW】**
1. **全身完整无裁剪**：通过。头顶、双手、双脚（运动鞋）完整，未超出画面边缘。
2. **背景是否绝对平面纯黑**：**未通过**。背景顶部为纯黑，但底部存在明显的灰蓝色地面反光/渐变（符合前置像素线索，底部两下角偏亮，有地面质感），不是绝对平面纯黑。
3. **脚掌贴近画面底缘，下方留白小**：**未通过**。脚掌下方有明显的地面反光区域，距离画面底缘有一定距离（留白/地面区域较大）。
4. **轮廓上是否有白色描边/白边残留**：通过。无明显白边。
5. **双手可见且手指清晰**：通过。双手握拳，手型正常，符合“正常人类战士”设定，非丧尸非变异。
6. **下半身是否明亮清晰**：通过。腿部、鞋子细节清晰，未融进黑背景（因为底部有地面反光衬托）。
7. **Cutout判定**：N/A（这是RAW图）。

**【抠图切割图 CUTOUT】**
1. **全身完整无裁剪**：通过。
2. **背景是否绝对平面纯黑**：**未通过**。背景主体为黑，但底部出现大面积不规则白色/亮色块（疑似抠图错误将地面高光保留或Alpha通道错误）。
3. **脚掌贴近画面底缘**：通过（人物本身位置没变，但底部被白色覆盖）。
4. **轮廓上是否有白色描边/白边残留**：**严重未通过**。人物脚部及周围有大面积锯齿状白色残留/白边，这是典型的抠图溢出或反选错误。
5. **双手可见**：通过。
6. **下半身**：通过（人物主体部分）。
7. **Cutout判定（背景全透明/干净）**：**严重未通过**。
   - 背景非全透明（显示为黑底+底部大白块，说明Alpha通道有问题，或者这是显示模式）。
   - 主体底部有大片白色杂质/残留（把地面反光全抠出来了）。
   - 轮廓极不干净，底部边缘呈锯齿状白色。
   - 无镂空大洞（除了底部被错误抠出的部分）。

**最终判定与修正建议：**

**RAW图判定：需重生成（或后期处理）。**
- 理由：背景不符合“绝对平面纯黑”设定，底部有地面反光和渐变。脚部下方留白（地面）过大。
- 修正建议：如果用于游戏立绘（通常需要黑底或透明底），需要去除底部的地面反光和渐变，将背景修成纯黑（#000000）。同时可以裁切掉底部多余的地面区域，让脚掌更贴近底边。

**CUTOUT图判定：需重生成（重抠图）。**
- 理由：抠图质量极差。底部出现了大面积的白色残留（疑似把地面的高光部分当成了前景保留，或者蒙版反转/阈值错误）。轮廓底部完全崩坏，呈锯齿状白色。
- 修正建议：
  1. 重新进行抠图。使用“选择主体”或“色彩范围”工具，选取人物。
  2. 由于RAW图底部有地面反光，抠图时容易把地面反光也抠出来（如图2所示）。建议先对RAW图进行“去背景”处理（将底部地面修掉，变成纯黑背景），然后再抠图。
  3. 或者在抠图时，仔细调整边缘，使用“删除”工具或蒙版，将底部的白色/灰色地面区域全部擦除，只保留人物主体。
  4. 确保最终Alpha通道正确，背景透明（或纯黑，视需求而定，但图2明显有白色错误残留）。

综合来看，这张图作为“健康亚洲青年男性战士”的立绘，人物生成质量很高（肌肉、衣服褶皱、表情都很棒），符合郑吒的设定。主要问题在于**背景处理**（RAW有地面反光，CUTOUT抠图失败）。

整理输出格式。
逐项核验。

**RAW图：**
1. 是。
2. 否（底部有地面反光/渐变）。
3. 否（下方有地面反光区域，留白较大）。
4. 否（无白边）。
5. 是（双手握拳，清晰）。
6. 是。
7. N/A。

**CUTOUT图：**
1. 是。
2. 否（底部有大片白色/亮色残留）。
3. 是（人物位置同RAW）。
4. 是（有严重白边残留，底部锯齿状白色）。
5. 是。
6. 是。
7. 否（背景不干净，有白色大块残留，轮廓底部崩坏）。

**最终判定：**
RAW：需修正（修背景）。
CUTOUT：需重抠。

修正建议：
1. RAW图：使用渐变工具或仿制图章，将底部灰蓝色地面反光/渐变消除，替换为纯黑背景（#000000）。裁切底部，使脚掌贴近底边。
2. CUTOUT图：基于修正后的RAW图（纯黑背景）重新抠图。或者在原RAW图基础上，使用快速蒙版，仔细擦除底部地面反光区域，只保留人物。确保Alpha通道纯净。

再仔细看图2，图2的背景其实是黑色的，但是底部有一大条白色的东西，看起来像是“选区”或者“蒙版”没处理好，把下面的地面高光显示为白色了。这绝对是抠图错误。

好，开始撰写回复。
角色设定：郑吒（无限恐怖主角），健康，战士。
图片内容确实很像郑吒（短发，健壮，严肃）。
T恤颜色深灰蓝，裤子深色工装/战术裤。符合。

逐项判定：

**【原始立绘 RAW】**
1. **全身完整无裁剪**：✅ 通过。头顶、双手（握拳）、双脚（运动鞋）均完整显示在画面内。
2. **背景是否绝对平面纯黑**：❌ 未通过。背景顶部为纯黑，但底部存在明显的灰蓝色地面反光/渐变（符合前置像素线索 lum≈51-55），并非绝对平面纯黑。
3. **脚掌贴近画面底缘，下方留白小**：❌ 未通过。脚掌下方有明显的深灰色地面反光区域，距离画面底缘有较明显的距离（留白/地面区域较大）。
4. **轮廓上是否有白色描边/白边残留**：✅ 通过。人物轮廓清晰，无明显白边或光晕残留。
5. **双手可见且手指清晰**：✅ 通过。双手自然下垂握拳，手型正常，符合“正常人类战士”设定，无变异特征。
6. **下半身是否明亮清晰**：✅ 通过。腿部、战术裤褶皱及鞋子细节清晰，未融进黑背景（得益于底部地面反光的衬托）。
7. **Cutout判定**：N/A（此为RAW图）。

**【抠图切割图 CUTOUT】**
1. **全身完整无裁剪**：✅ 通过。人物主体完整。
2. **背景是否绝对平面纯黑**：❌ 未通过。背景主体为黑，但底部出现大面积不规则白色/亮色块（严重抠图错误）。
3. **脚掌贴近画面底缘**：✅ 通过（人物主体位置未变，但底部被错误区域覆盖）。
4. **轮廓上是否有白色描边/白边残留**：❌ 严重未通过。人物脚部及周围（特别是两脚之间及外侧）有大面积锯齿状白色残留/白边，这是典型的抠图溢出或Alpha通道错误。
5. **双手可见**：✅ 通过。
6. **下半身**：✅ 通过（人物主体腿部清晰）。
7. **Cutout判定（背景全透明/干净）**：❌ 严重未通过。
   - **背景不干净**：底部有大片白色/亮色杂块（疑似将RAW图底部的地面高光错误地保留为前景，或Alpha通道反转/阈值错误）。
   - **轮廓不干净**：底部边缘呈剧烈锯齿状白色，完全崩坏。
   - **无镂空大洞**：主体人物部分无大洞，但底部被错误区域干扰。

**最终判定与修正建议**

**【原始立绘 RAW】判定：需修正（后期处理）**
*   **判定理由**：人物生成质量极高，完美符合“健康亚洲青年男性战士（郑吒）”设定（肌肉线条、服装材质、神态均在线）。但**背景不符合要求**，底部有地面反光和渐变，且脚部下方留白（地面）过大，不符合游戏立绘通常需要的“纯黑背景”或“紧凑构图”。
*   **修正建议**：
    1.  **去背景/修底**：使用渐变工具或仿制图章，将底部灰蓝色的地面反光和渐变消除，向下延伸覆盖为纯黑背景（#000000）。
    2.  **裁切**：裁切掉底部多余的地面区域，使脚掌（鞋底）贴近画面底边缘，减少下方留白。

**【抠图切割图 CUTOUT】判定：需重生成（重抠图）**
*   **判定理由**：抠图质量**极差**。图2底部出现了大面积的白色锯齿状残留，这明显是抠图时“选择主体”或“色彩范围”阈值设置不当，把RAW图底部的地面高光（亮部）全部抠出来当成了前景，或者Alpha通道显示错误。这导致人物底部“长”出了白色的地皮，完全无法用于游戏合成。
*   **修正建议**：
    1.  **先修RAW**：建议先按照上述RAW的修正建议，将底部地面修成纯黑，裁切紧凑。
    2.  **重新抠图**：在纯黑背景的RAW图上进行抠图。使用“选择主体”功能，或者使用“快速选择工具”/“钢笔工具”仔细勾勒人物边缘。
    3.  **处理边缘**：由于原图底部有反光，抠图时容易残留边缘。可以使用“选择并遮住”（Select and Mask）功能，调整边缘半径，使用“清除”工具去掉底部残留的地面像素。
    4.  **最终检查**：导出为PNG（透明背景），检查Alpha通道，确保底部没有白色/灰色残留，人物与透明背景过渡自然。

**合成最终判定**：
目前两张图均**不可直接发布/合成**。
RAW图背景有瑕疵（地面反光），CUTOUT图抠图彻底失败（底部大面积白边/杂色）。
必须**先修正RAW图背景（去地面反光变纯黑）并裁切**，然后**重新进行高质量的抠图**，才能得到可用的CUTOUT图。

人物本身（模型/贴图）质量很好，是“好图”被“坏背景/坏抠图”耽误了。
郑吒（Zhongcha）... 哦是郑吒 (Zheng Cha)。无限恐怖。
设定完全吻合。

OK，逻辑通顺。
---usage: {"completion_tokens": 4000, "prompt_tokens": 1982, "total_tokens": 5982, "completion_tokens_details": {"reasoning_tokens": 3871, "text_tokens": 129}, "prompt_tokens_details": {"cached_tokens": 0, "text_tokens": 442, "image_tokens": 1540}}

===== hunter_wan3 :: OK
content: 作为资深游戏美术质检员，针对这张“无皮肤肌肉怪兽(hunter撕裂者)”的角色立绘，我已根据设定要求（灰棕肌肉块面、无衣物、特定武器、低重心姿态等）及前置像素线索进行了详细核验。

以下是逐项判定报告：

### 一、【原始立绘 RAW】核验

1.  **全身完整无裁剪**：✅ **通过**。头顶、左手（带刺巨爪）、右手（骨刃）、双脚均完整呈现。左脚贴近底缘，右脚因扑击姿态悬空，符合解剖与动态逻辑，无关键部位裁切。
2.  **背景是否绝对平面纯黑**：✅ **通过**。背景呈现纯净的黑色（#000000或极深灰），占比约61%（符合设定），无反射、投影、渐变或光晕干扰。
3.  **脚掌贴近画面底缘**：✅ **通过**。左脚掌（画面左下）非常贴近底缘，下方黑色留白极小，视觉重心稳定。
4.  **轮廓上是否有白色描边/光晕**：✅ **通过**。主体为灰白色/灰棕色肌肉块面，与黑色背景形成高对比，边缘锐利清晰，无多余的白色描边或浅蓝光晕（主体自身的明暗交界线属于正常体积感，非瑕疵）。
5.  **爪/刀清晰分离**：✅ **通过**。左手（画面左侧）的带刺巨爪手指/尖刺清晰分离，右手（画面右侧）的刀形骨刃与手部结构清晰，无糊状融合。
6.  **下半身明亮清晰**：✅ **通过**。腹、腿、脚均为明亮的灰白色肌肉纹理，对比度高，完全未融进黑色背景，无“黑剪影”现象，符合“非人类/无皮肤”的质感设定。

**🔴 RAW 判定结果：【可发布】**
*原始图质量极高，光影、解剖结构、材质（灰棕肌肉块面）均完美符合设定，背景干净，可直接用于游戏资产或作为高质量底图。*

---

### 二、【抠图切割图 CUTOUT】核验

1.  **全身完整无裁剪**：✅ **通过**（主体内容完整）。
2.  **背景是否绝对平面纯黑/透明**：❌ **失败**。背景虽显示为白色（预览底），但存在严重的**背景残留**。
3.  **脚掌贴近画面底缘**：✅ **通过**。
4.  **轮廓上是否有白色描边/光晕**：❌ **失败**。主体轮廓周围存在明显的**深色/黑色边缘残留**（黑边），这是抠图时边缘反选或抗锯齿处理不当造成的。
5.  **爪/刀清晰分离**：✅ **通过**。
6.  **下半身明亮清晰**：✅ **通过**。
7.  **Cutout 专项判定**：
    *   **背景全透明**：❌ **失败**。在画面左侧（角色右臂/左臂内侧，即画面左边的腋下至腰部区域），有一大块**纯黑色残留**（对应RAW图中的黑色背景）。这说明抠图算法未能识别该处为背景，或者错误地保留了黑色背景块。
    *   **主体完整无镂空大洞**：❌ **失败**。上述黑色残留块破坏了背景的通透性。如果那是透明区域，应该显示为背景色（白）；如果是黑色，说明是背景没抠干净。
    *   **无碎点杂质**：❌ **失败**。图片最左侧边缘有一列垂直的**虚线/点状杂讯**（看起来像裁剪错误或传输损坏），这是明显的瑕疵。
    *   **轮廓干净平滑**：❌ **失败**。主体边缘（特别是腿部和头部）有一圈细细的黑/灰边（Rim残留），轮廓不够干净平滑。

**🔴 CUTOUT 判定结果：【需重生成/重抠图】**
*抠图质量严重不合格。存在大块背景残留（左臂内侧黑块）、边缘黑边污染、以及左侧边缘的杂讯损坏。*

---

### 三、合成最终判定与修正建议

**最终判定：**
*   **RAW图**：**✅ 合格**。可直接用于游戏开发、宣传或作为高质量素材。
*   **CUTOUT图**：**❌ 不合格**。无法直接用于合成，必须重新处理。

**修正建议（针对CUTOUT）：**
1.  **重新抠图（Masking）**：
    *   使用更精细的抠图工具（如Photos
reasoning_content: 用户希望我作为资深游戏美术质检员，对提供的两张图片（原始RAW图和抠图CUTOUT图）进行详细的质检。

**设定回顾：**
角色：无皮肤肌肉怪兽 (hunter撕裂者)
特征：灰棕肌肉块面、无衣物、左手巨大钩/刀爪、右手刀形骨刃、低重心扑击猎杀姿态，非人类。
前置线索：底部角落纯净纯黑，背景黑占比略低 (bg_dark ≈ 0.61)。重点核实背景是否有白/浅蓝边缘光晕或纯白描边残留，以及下半身是否明亮无黑剪影。

**检查项 (7点)：**
1. 全身完整无裁剪 (头顶/双手/脚完整; 脚可被画面底缘轻微裁切)
2. 背景是否绝对平面纯黑, 无反射/投影/渐变/光晕
3. 脚掌贴近画面底缘, 下方留白小
4. 轮廓上是否有白色描边/白边残留/白或浅蓝边缘光晕
5. (hunter) 爪/刀清晰分离、不糊不融合
6. 下半身(腿/脚/下躯干)是否明亮清晰、不融进黑背景(hunter 尤其无黑剪影)
7. cutout: 背景全透明、主体完整无镂空大洞、无白边/黑边残留、无碎点杂质、轮廓干净平滑

**图片分析：**

**图片1：原始立绘 RAW**
- 角色是一个灰白色的肌肉怪物，背景是黑色的。
- 1. 全身完整：头顶完整，左手（带刺爪）完整，右手（带刀）完整，左脚完整，右脚完整。脚底没有明显被裁切，甚至留有一点黑边。-> 通过。
- 2. 背景：背景是纯黑色的。看起来比较干净，没有明显的渐变或光晕。-> 通过。
- 3. 脚掌：左脚掌贴近底缘，右脚掌悬空（因为是扑击姿态）。左脚下方留白（黑边）很小。-> 通过。
- 4. 轮廓：仔细看边缘，特别是左手爪子、右手刀、背部尖刺。边缘比较锐利，是灰白色的主体和黑色背景的交界。没有明显的白色描边或浅蓝光晕。主体本身就是灰白色的，所以边缘看起来是灰白色的。-> 通过（或者说符合设定，主体是灰白色，背景是黑色，边缘清晰）。
- 5. 爪/刀：左手是巨大的带刺爪子，手指/爪子清晰分离。右手拿着刀形骨刃，刀和手清晰分离。-> 通过。
- 6. 下半身：腿部和脚部都是灰白色的肌肉，非常明亮，没有融进黑色背景。-> 通过。
- 7. Cutout检查项不适用RAW。

**图片2：抠图切割图 CUTOUT**
- 背景变成了白色（通常抠图透明背景在白色底板上显示为白色，或者这里直接就是白底）。等等，看图片，背景是白色的。
- 1. 全身完整：同RAW。-> 通过。
- 2. 背景：背景是白色的。这不符合“背景绝对平面纯黑”。但在CUTOUT语境下，通常意味着背景被去除了。看左臂和身体之间，有一大块黑色的区域！这是镂空或者抠图错误。左臂和躯干之间有一块黑色的三角形区域。这说明抠图把背景（黑色）保留了一部分，或者把主体切掉了。
    - 仔细看RAW图，左臂（带刺的那只，画面左侧）和躯干之间是黑色的背景。
    - 在CUTOUT图中，左臂和躯干之间有一大块黑色的区域。这说明抠图算法把这块黑色背景当成了主体的一部分？或者反过来，把主体的一部分切掉了？
    - 不，看RAW图，左臂和躯干之间是黑色的背景空隙。
    - 在CUTOUT图中，这个空隙变成了黑色。这意味着抠图没有把背景完全去掉，或者把背景留下来了。如果是透明背景，这里应该是透明的（白色显示）。这里显示为黑色，说明抠图失败，保留了黑色背景块。这是一个大洞/残留。
    - 再看边缘，有一圈细细的黑线/灰线。
- 3. 脚掌：同RAW。
- 4. 轮廓：边缘有一圈很细的深色/黑色描边残留。特别是在腿部、头部。
- 5. 爪/刀：同RAW。
- 6. 下半身：明亮。
- 7. Cutout判定：
    - 背景全透明？看起来像白底，但左臂和身体之间有大块黑色残留（这是RAW里的背景黑）。这说明抠图没抠干净，或者反选错了。这块黑色区域（左臂内侧到腰部）在RAW里是背景，在CUTOUT里是黑色块。这是严重的抠图错误（背景残留/镂空错误）。
    - 主体完整无镂空大洞：左臂和身体之间那块黑色的如果是背景，那就是抠图漏了（没抠掉）。如果是主体，那就是主体镂空了（把肉切掉了）。看RAW，那是背景。所以CUTOUT里这里应该是透明（或白），现在是黑色，说明**背景残留/抠图不干净**。
    - 无白边/黑边残留：边缘有一圈明显的深色轮廓线（可能是抗锯齿或者抠图边缘的残留）。
    - 轮廓干净平滑：边缘看起来有点锯齿或者不自然，特别是左侧边缘有一排虚线/点状物（可能是图片压缩或者裁剪边缘的问题）。看最左边，有一列垂直的虚线/点。这是裁剪或传输问题。

**重新仔细对比RAW和CUTOUT：**
RAW图：背景纯黑。主体灰白。
CUTOUT图：背景白。主体灰白。
问题点：
1. CUTOUT图中，左手（画面左侧，带刺）和身体之间，有一大块黑色的区域。在RAW图中，这个位置是黑色背景。这意味着CUTOUT没有把背景去掉，或者把背景留下来了。这属于“背景残留”或“抠图不彻底”。
2. CUTOUT图的最左侧边缘，有一列垂直的虚线/点状物。这是明显的瑕疵。
3. CUTOUT图的轮廓周围有一圈细细的深色/黑色边缘（黑边残留），这是抠图常见的边缘污染。

**逐项判定草稿：**

**【原始立绘 RAW】**
1. 全身完整：是。头顶、双手（左爪右刀）、双脚都在画面内。
2. 背景纯黑：是。背景看起来非常纯净的黑色。
3. 脚掌贴近底缘：左脚贴近，右脚悬空。符合低重心姿态。
4. 轮廓白描边：无。主体是灰白色，背景黑，边缘清晰。
5. 爪/刀分离：左手大爪清晰，右手刀清晰。
6. 下半身明亮：是，灰白色肌肉，对比度高，不融进黑背景。
7. N/A。

**【抠图切割图 CUTOUT】**
1. 全身完整：是。
2. 背景纯黑（这里指抠图后的背景状态，通常应为透明）：这里显示为白色背景（可能是预览），但关键问题是**左臂与躯干之间有大块黑色残留**（这是RAW的背景黑）。说明抠图失败，背景未完全去除。
3. 脚掌：同RAW。
4. 轮廓白描边/光晕：边缘有细微的黑/灰边残留（抠图边缘不干净）。
5. 爪/刀：分离清晰。
6. 下半身：明亮。
7. Cutout专项：
    - 背景全透明：否。左臂和身体之间有大块黑色（背景残留）。最左侧有虚线瑕疵。
    - 主体完整无镂空：左臂和身体之间的黑色块如果是背景没抠掉，那就是抠图不干净。
    - 无白边/黑边残留：有黑边残留（主体周围有一圈深色细边）。
    - 无碎点杂质：左侧边缘有垂直虚线/杂点。
    - 轮廓干净平滑：边缘有黑边，不够干净。

**修正建议：**
RAW图看起来质量很高，可以直接用。
CUTOUT图有问题。
1. 左臂（画面左侧）和躯干之间的黑色背景块没有抠掉（或者反了，把背景留下来了）。需要重新抠图，使用“选择主体”或“移除背景”，并填充该黑色区域为透明。
2. 左侧边缘的垂直虚线/杂点需要清除。
3. 主体边缘的黑/灰边残留（抗锯齿问题）需要用“去色”或“调整边缘”工具清理，或者重新生成抠图（使用更精细的Mask）。

**再次检查前置像素线索：**
"底部角落纯净纯黑, 但背景黑占比略低(bg_dark≈0.61)。请重点核实背景是否仍有白/浅蓝边缘光晕或纯白描边残留, 以及下半身(腹/腿/脚)是否明亮无黑剪影。"
- RAW图：背景黑占比确实大概60%左右。底部角落纯黑。下半身（腿/脚）非常明亮（灰白色），没有黑剪影。符合设定。
- CUTOUT图：因为背景被去除了（变成了白底预览），所以无法直接看背景黑占比，但可以看出抠图质量很差。

**最终判定逻辑：**
RAW：可发布。质量很好，符合“无皮肤肌肉怪兽”、“灰棕肌肉块面”（这里是灰白/灰棕）、“左手巨大钩”、“右手刀”、“低重心”。
CUTOUT：需重生成/重抠图。
- 错误1：左臂内侧（画面左侧腋下/腰侧）有一大块黑色背景未抠除（在CUTOUT图中显示为黑色块，因为透明背景在白色画布上显示为白，黑色就是残留的黑色像素）。
- 错误2：左侧边缘有垂直的虚线/噪点。
- 错误3：主体边缘有黑边/灰边残留（Rim light残留或抠图边缘污染）。

让我们仔细看CUTOUT的左臂和身体之间。
RAW图：左臂（带刺）和身体之间是黑色的。
CUTOUT图：左臂和身体之间是黑色的。
这说明抠图算法把“黑色背景”当成了“主体”的一部分保留了下来？或者把“非主体”的透明区域填成了黑色？
通常CUTOUT是透明背景。如果显示为白色，说明是白底图。
如果CUTOUT图里那块黑色是黑色的像素（RGB 0,0,0），那就是背景残留。
如果是透明像素，在白色画布上应该显示为白色。
所以那块黑色是**背景残留**。

再看CUTOUT图的最左边，有一排垂直的点。这是明显的图像损坏或裁剪错误。

**总结：**
RAW图：完美。
CUTOUT图：严重瑕疵。背景未去净（大块黑色残留），边缘有黑边，左侧有杂点。

**撰写回复：**
按照要求的格式，逐项判定。

**RAW判定：**
1. 完整：是。
2. 背景纯黑：是。
3. 脚掌：是。
4. 轮廓：无白边（主体本身就是浅色，背景黑，边缘自然）。
5. 爪/刀：清晰。
6. 下半身：明亮，无剪影。
结论：可发布。

**CUTOUT判定：**
1. 完整：是（视觉上）。
2. 背景：否。有大块黑色背景残留（左臂与躯干之间）。
3. 脚掌：是。
4. 轮廓：有黑边/灰边残留。
5. 爪/刀：清晰。
6. 下半身：明亮。
7. Cutout专项：
   - 背景全透明：否（有黑色残留块）。
   - 主体无镂空：否（那块黑色如果是背景没抠掉，就是抠图失败；如果是主体被切了，就是镂空。看RAW那是背景，所以是背景残留）。
   - 无碎点：否（左侧边缘有虚线）。
   - 轮廓平滑：否（有黑边）。
结论：需重生成/重抠图。

**合成最终判定：**
RAW可用。CUTOUT不可用，需修复。
修正建议：重新抠图，修复左臂内侧黑色残留，清理左侧边缘噪点，去除主体边缘黑边。

等等，再仔细看CUTOUT图。
CUTOUT图背景是白色的。
左臂和身体之间的黑色块。在RAW里那是背景（黑色）。
在CUTOUT里，如果抠图正确，那里应该是透明（显示为白色）。
现在那里是黑色。说明**抠图把背景留下来了**，或者**把背景填成了黑色**。
这是一个巨大的错误。

另外，CUTOUT图里，主体的轮廓有一圈细细的深色线。这是“黑边残留”。
通常抠图会有白边（因为主体亮，背景黑，边缘像素混合）。这里反而有黑边？
可能是RAW图本身主体边缘有暗部（阴影），抠图时把暗部边缘保留了，或者反选时把背景的一点点暗部保留了。
看RAW图，主体边缘确实有阴影（深灰色）。抠图如果只取亮部，边缘会虚。如果取全，边缘就是深灰色。
CUTOUT图里，主体周围有一圈明显的深色轮廓，甚至有点像描边。

好吧，开始写正式回复。
---usage: {"completion_tokens": 4000, "prompt_tokens": 1983, "total_tokens": 5983, "completion_tokens_details": {"reasoning_tokens": 3000, "text_tokens": 1000}, "prompt_tokens_details": {"cached_tokens": 0, "text_tokens": 443, "image_tokens": 1540}}

===== guard_wan1 :: OK
reasoning_content: 用户希望我作为资深游戏美术质检员，对提供的两张图片（原始立绘 RAW 和 抠图切割图 CUTOUT）进行逐项核验。

**角色设定分析：**
- 角色：人类守卫 (human guard)
- 装备：防暴装备/制服 (riot gear/uniform)，黑色防暴甲 (black riot armor vest)，战术背心 (tactical vest)，肩甲 (shoulder pauldrons)。
- 武器/道具：防暴盾 (riot shield)，短棍 (short baton)。
- 姿态：立姿警戒 (standing alert guard stance)。
- 排除项：非丧尸 (NOT a zombie)，非主角 (not the protagonist)。

**前置像素线索检查：**
- 底部角落纯净纯黑（针对RAW图）。
- 全身完整、脚掌贴底、无白描边、手/盾清晰分离、下半身明亮。
- 抠图干净无镂空/杂点/白黑边。

**核验项目（7点）：**
1. 全身完整无裁剪（头顶/双手/脚完整；脚可被画面底缘轻微裁切）。
2. 背景是否绝对平面纯黑，无反射/投影/渐变/光晕。
3. 脚掌贴近画面底缘，下方留白小。
4. 轮廓上是否有白色描边/白边残留/白或浅蓝边缘光晕。
5. 双手可见且手指清晰分离；（这里是守卫，不是hunter，所以看手和武器分离）。
6. 下半身（腿/脚/下躯干）是否明亮清晰、不融进黑背景。
7. cutout: 背景全透明、主体完整无镂空大洞、无白边/黑边残留、无碎点杂质、轮廓干净平滑。

**图片分析：**

**图片 1: 原始立绘 RAW**
- **内容：** 一个穿着黑色防暴装备的男性守卫，戴着头盔和面罩，手持短棍和防暴盾。背景是黑色的。
- **1. 全身完整无裁剪：** 头顶完整，双手（左手拿盾，右手拿棍）完整，脚完整。看起来全身都在画面内。判定：通过。
- **2. 背景是否绝对平面纯黑：** 背景看起来非常黑，但仔细看有一些噪点或者极微弱的渐变，不过整体接近纯黑。等等，看描述“底部角落纯净纯黑”。背景看起来是纯黑的，没有明显的反射或投影。但是，人物本身有光照，背景是纯黑。判定：基本通过（可能有极细微噪点，但作为设定背景算纯黑）。
- **3. 脚掌贴近画面底缘：** 脚底离底边还有一段距离，留白不小。判定：通过（留白适中，不算贴底，但也没被裁切）。等等，要求是“脚掌贴近画面底缘，下方留白小”。这里脚底离底边还有一段距离。让我们再看一眼图。脚底离底边大概有5-10%的距离。不算特别贴底，但也算合理。不过通常游戏立绘希望贴底。这里留白稍微有点大。
- **4. 轮廓上是否有白色描边/白边残留/白或浅蓝边缘光晕：** 人物边缘比较清晰，没有明显的白边。面罩边缘有一些高光，这是材质反射，不是描边。判定：通过。
- **5. 双手可见且手指清晰分离：** 右手（画面左侧）拿着短棍，手指握着棍子，清晰。左手（画面右侧）拿着盾牌，被盾牌挡住了一部分，但手臂连接处清晰。短棍和手分离清晰。判定：通过。
- **6. 下半身是否明亮清晰：** 下半身是深灰色/黑色的盔甲和裤子，在黑色背景下，对比度较低。特别是大腿和裤子部分，几乎和背景融为一体（黑色融进黑色）。这是一个问题。设定要求“下半身明亮清晰、不融进黑背景”。这里的裤子是深黑色的，背景也是黑色的。虽然盔甲有高光，但整体下半身偏暗，容易和背景混淆。不过看图片，腿部的盔甲（护膝、护胫）是有高光的，能看清。裤子部分比较暗。判定：勉强通过，但对比度略低，有融入背景的风险（特别是裤子部分）。
- **7. Cutout检查（针对图1）：** 图1背景是黑色的，不是透明的。这是RAW图。

**图片 2: 抠图切割图 CUTOUT**
- **内容：** 同样的守卫，背景变成了白色（或者是透明显示为白色）。
- **1. 全身完整无裁剪：** 同上，完整。
- **2. 背景：** 背景是白色的。这是抠图后的结果。
- **3. 脚掌：** 同上。
- **4. 轮廓：** 边缘看起来比较干净。
- **5. 手/盾：** 同上。
- **6. 下半身：** 在白色背景下，下半身非常清晰，黑色盔甲对比强烈。
- **7. Cutout判定：**
    - 背景全透明（显示为白底，通常是透明通道被白色填充或者就是白底图）。如果是透明背景，这里显示为白色，可能是查看器的问题。假设它是透明背景（Alpha通道）。
    - 主体完整无镂空大洞：看起来完整。
    - 无白边/黑边残留：边缘看起来比较干净。
    - 无碎点杂质：看起来干净。
    - 轮廓干净平滑：是的。

**重新仔细审视要求：**
- 设定：穿着防暴装备/制服的人类守卫(guard): 黑色防暴甲、战术背心、肩甲, 持防暴盾与短棍, 立姿警戒, 非丧尸非主角。
- 前置像素线索: 底部角落纯净纯黑。请核实全身完整、脚掌贴底、无白描边、手/盾清晰分离、下半身明亮、抠图干净无镂空/杂点/白黑边。

**针对 RAW 图 (图1) 的详细检查：**
1.  **全身完整：** 是。头顶、手脚都在。
2.  **背景纯黑：** 是。背景是纯黑 `#000000` 或接近。
3.  **脚掌贴底：** 脚底离底边还有一段距离。要求“脚掌贴近画面底缘，下方留白小”。这里留白大概有 5-10%。不算特别贴底，但也没裁切。可以算通过，或者提示留白略大。
4.  **白描边：** 无明显白边。
5.  **手/盾分离：** 右手握棍，清晰。左手持盾，盾牌很大，挡住了左手大部分，但手臂可见。短棍和手分离清晰。
6.  **下半身明亮：** 这是一个关键点。角色穿的是“黑色防暴甲”。在黑色背景下，黑色的裤子和黑色的背景会融合。看图，裤子部分（大腿之间、腹部下方）非常暗，几乎和背景融为一体。虽然护膝和护胫有高光，但整体下半身（特别是躯干下部）对比度很低。这违反了“下半身明亮清晰、不融进黑背景”的前置线索要求。虽然设定是黑色防暴甲，但在黑色背景下必须打光让下半身可见。这里下半身确实有点暗，特别是裤子部分。
7.  **CUTOUT (图2) 检查：**
    - 背景：白色（代表透明）。
    - 主体完整：是。
    - 镂空：无。
    - 白边/黑边：边缘看起来有一点点极细的白边（因为抠图通常会有抗锯齿），但在白底上看不出来。如果是透明背景，边缘应该干净。
    - 碎点：无。

**修正建议：**
- RAW图中，下半身（裤子部分）太暗，与黑色背景融合严重。需要提亮下半身或增加对比度，确保在黑色背景下也能看清腿部细节。
- 脚底留白略大，可以裁剪或重新生成让脚更贴底。

**再次检查图1（RAW）：**
- 背景：纯黑。
- 下半身：裤子是深灰/黑色。护腿是深灰色。在纯黑背景下，确实有点“融进去”的感觉，尤其是两腿之间和腹部下方。
- 手：右手（画面左侧）拿着短棍。手指握着。清晰。
- 盾：画面右侧的大盾牌。

**再次检查图2（CUTOUT）：**
- 背景是白色的。这说明抠图把黑色背景去掉了，变成了白色（或者透明显示为白）。
- 边缘：看头盔边缘，比较清晰。
- 下半身：在白色背景下非常清晰，黑色盔甲轮廓分明。

**综合判定逻辑：**
- **RAW图：**
    - 1. 全身完整：Yes。
    - 2. 背景纯黑：Yes。
    - 3. 脚掌贴底：No，留白较多（约10%）。
    - 4. 白描边：No。
    - 5. 手/盾：Yes，清晰。
    - 6. 下半身明亮：No/Partial。设定是黑色防暴甲，但在纯黑背景下，下半身（裤子）太暗，几乎与背景融合。虽然盔甲有高光，但整体偏暗。前置线索要求“下半身明亮”。这里下半身不够明亮。
    - 7. (RAW不适用，看CUTOUT)

- **CUTOUT图：**
    - 1-6. 对应RAW。
    - 7. Cutout判定：背景全白（透明），主体完整，无大洞，无白边（在白色背景下看不出，但在透明下应该检查），无碎点。轮廓看起来不错。

**等等，仔细看前置线索：**
“底部角落纯净纯黑。请核实全身完整、脚掌贴底、无白描边、手/盾清晰分离、下半身明亮、抠图干净无镂空/杂点/白黑边。”
这意味着RAW图必须满足：下半身明亮（不融进黑背景）。
现在的RAW图，下半身（裤子）是黑的，背景是黑的。这明显**不满足**“下半身明亮清晰、不融进黑背景”。
虽然角色设定是“黑色防暴甲”，但美术要求里写了“下半身明亮”。这说明生成时打光不够，或者颜色太深。
看图1，护腿（shin guards）是灰蓝色的，有高光，还算亮。但是裤子（pants）是深黑色的，和背景混在一起了。
所以RAW图判定：**需重生成**（因为下半身融入背景，且脚底留白大）。

**CUTOUT图判定：**
抠图是基于RAW图做的。既然RAW图下半身融入背景，抠图的时候可能会把下半身暗部抠掉，或者保留下来但边缘不清晰。
看图2，下半身（裤子部分）是黑色的，在白色背景下很清晰。说明抠图把黑色主体都抠出来了。
但是，RAW图里下半身太暗是个问题。
CUTOUT图本身质量：
- 背景：白（透明）。
- 轮廓：干净。
- 主体：完整。
CUTOUT判定：**可发布**（作为素材），但基于RAW的问题，合成时需要注意。

**修正建议：**
1.  **RAW图：** 下半身（特别是裤子区域）太暗，与黑色背景融合。需要加强下半身打光（Rim light或Key light），使黑色盔甲在黑色背景下能看出轮廓和细节（即“下半身明亮”）。
2.  **RAW图：** 脚底留白过多，建议裁剪底部或重新生成让脚掌贴底。
3.  **CUTOUT图：** 抠图质量尚可，但因为RAW下半身暗，抠图边缘在暗部可能不完美（虽然图2看起来还行，因为背景变白了对比度高了）。

**最终结论结构：**
- 逐项核验 RAW。
- 逐项核验 CUTOUT。
- 最终判定与建议。

**详细逐项：**

**【原始立绘 RAW】 (图1)**
1.  **全身完整：** 是。头顶、双手、双脚完整。
2.  **背景纯黑：** 是。背景纯黑，无反射。
3.  **脚掌贴底：** **否**。脚底距离画面底部有约10%的留白，未贴底。
4.  **白描边/光晕：** 否。轮廓清晰，无明显白边或光晕（面罩高光是材质）。
5.  **手/盾分离：** 是。右手握短棍清晰，左手持盾（盾遮挡手，但手臂连接清晰），短棍与手分离清晰。
6.  **下半身明亮：** **否/瑕疵**。角色设定为黑色防暴甲，但在纯黑背景下，下半身（特别是战术裤/下躯干）过暗，与黑色背景几乎融为一体，缺乏足够的明暗对比来凸显轮廓，不符合“下半身明亮清晰”的前置线索要求。护膝和护胫有高光，但整体偏暗。
7.  **Cutout判定 (N/A，看图2)：** 背景非透明（是黑色）。

**【抠图切割图 CUTOUT】 (图2)**
1.  **全身完整：** 是。
2.  **背景：** 白色（代表透明通道）。
3.  **脚掌：** 同上，留白较大。
4.  **轮廓：** 干净。
5.  **手/盾：** 清晰。
6.  **下半身：** 在白色背景下非常清晰，黑色主体对比强烈。
7.  **Cutout质量：** 背景全白（透明），主体完整，无大镂空。无碎点。轮廓平滑。无明显白边/黑边残留（因为背景是白的，边缘看起来有点硬，但还算干净）。

**最终判定：**
- **RAW：** **需重生成**。主要问题：下半身（裤子部分）太暗，融入黑色背景（违反“下半身明亮”线索）；脚底留白过大。
- **CUTOUT：** **可发布（临时使用）/ 需修正**。抠图本身比较干净，但基于RAW的缺陷，建议重新生成RAW（调整打光让下半身可见，调整构图让脚贴底），然后重新抠图。

**修正建议：**
1.  **重生成RAW：** 保持角色设定（黑色防暴甲），但必须**加强下半身打光**（例如增加侧面轮廓光或底部反光），确保黑色裤子和盔甲在黑色背景下能清晰分辨，不融入背景。
2.  **构图调整：** 裁剪底部或重新生成，使**脚掌贴近画面底缘**，减少下方留白。
3.  **重新抠图：** 基于修正后的RAW图重新进行抠图，确保边缘干净。

再仔细看图1的脚。脚底确实离底边有距离。
再仔细看图1的下半身。裤子是深灰/黑。背景是纯黑。确实融合。
再看图2。图2是白底。说明抠图把黑背景去掉了。
图2的边缘：看盾牌右侧，边缘有点锯齿？不，挺平滑的。
看头盔顶部，挺平滑。

**关于“下半身明亮”：**
设定里写了“黑色防暴甲”。
前置线索写了“下半身明亮”。
这就矛盾了，除非打光很强。
图1里，护腿（shin guards）是灰蓝色的，挺亮。但是裤子（pants）是黑的。
两腿之间的阴影太深。
所以判定为“需重生成”是合理的。

**关于“脚掌贴底”：**
图1脚底离底边大概100像素（假设图高1000）。留白挺大。
判定为“需重生成”或“需裁剪”。

**总结输出内容：**
按照用户要求的格式，逐项判定。

**RAW图判定：**
1. 全身完整：✅ 通过。
2. 背景纯黑：✅ 通过。
3. 脚掌贴底：❌ 未贴底，下方留白较大（约10%）。
4. 白描边：✅ 无。
5. 手/盾：✅ 清晰分离。
6. 下半身明亮：❌ **不通过**。下半身（战术裤/下躯干）过暗，与纯黑背景融合严重，不符合“下半身明亮清晰”要求。虽然护甲有高光，但整体暗部细节丢失。
7. Cutout（RAW不适用，略过或说明背景非透明）。

**CUTOUT图判定：**
1. 全身完整：✅ 通过。
2. 背景：白色（透明）。
3. 脚掌：同上，留白大。
4. 轮廓：✅ 干净。
5. 手/盾：✅ 清晰。
6. 下半身：✅ 在透明/白底背景下清晰可见（黑色主体对比度高）。
7. Cutout质量：✅ 通过。主体完整，无大洞，无碎点，轮廓平滑。

**最终判定：**
- RAW：需重生成（下半身融入背景，脚底留白大）。
- CUTOUT：需重抠图（基于修正后的RAW）。

**修正建议：**
1. 调整RAW图打光：增加下半身（腿部、躯干下部）的照明或轮廓光，使黑色防暴装备在黑色背景下具有足够的层次和亮度，避免“融进黑背景”。
2. 调整构图：裁剪底部或重新生成，使脚掌贴近底边。
3. 重新抠图。

OK，逻辑通顺。开始撰写回复。
注意语气：资深游戏美术质检员。
格式：逐项核验。

再仔细看一下图1的右手（画面左侧）。
右手拿着短棍。短棍是灰色的。手是戴着手套的（深灰色）。
手指握着短棍。清晰。
左手（画面右侧）拿着盾。盾挡住了手。
手臂连接盾。清晰
---usage: {"completion_tokens": 4001, "prompt_tokens": 1968, "total_tokens": 5969, "completion_tokens_details": {"reasoning_tokens": 4000, "text_tokens": 1}, "prompt_tokens_details": {"cached_tokens": 0, "text_tokens": 428, "image_tokens": 1540}}

===== zombie_wan1 :: OK
content: 作为一名资深游戏美术质检员，我将根据您提供的设定（腐坏人类丧尸）及前置像素线索，对【原始立绘 RAW】与【抠图切割图 CUTOUT】进行逐项严格核验。

### 📊 核验报告

#### 1. 【原始立绘 RAW】 (图1：黑底带白边版)

| 序号 | 核验项目 | 判定结果 | 详细说明 |
| :--- | :--- | :--- | :--- |
| **1** | **全身完整无裁剪** | ✅ **通过** | 头顶头发完整，双手手指完整（未裁剪），脚掌完整（贴近底缘）。 |
| **2** | **背景是否绝对平面纯黑** | ❌ **不通过** | 背景主体虽为黑色，但角色周围存在**极粗的白色描边/光晕**，破坏了背景的纯净度，且白色区域过大，不属于正常的背景渐变或光效。 |
| **3** | **脚掌贴近画面底缘** | ✅ **通过** | 脚掌贴近底缘，下方留白极小，符合直立姿态设定。 |
| **4** | **轮廓上是否有白色描边/白边残留** | ❌ **严重不通过** | **存在非常明显的粗白色描边**（约2-4px宽），环绕全身。这是典型的AI生成“贴纸”效果或自动抠图错误，严重违反“无白描边”要求。 |
| **5** | **双手可见且手指清晰分离** | ✅ **通过** | 双手自然下垂，手指关节清晰，未发生融合（糊在一起），符合丧尸特征。 |
| **6** | **下半身(腿/脚/下躯干)是否明亮清晰** | ✅ **通过** | 下半身衣物纹理（破洞、血迹）清晰，灰蓝色调明亮，未融进黑色背景（虽有白边隔离，但主体本身明亮）。 |
| **7** | **Cutout相关** | - | RAW图不适用此项。 |

**RAW 判定：❌ 需重生成 / 需严重修图**
**原因**：RAW图带有巨大的白色描边，这通常是AI生成时的默认边界或提示词未限制导致的。作为游戏素材，这种带白边的RAW图无法直接使用，必须去除白边或重新生成。

---

#### 2. 【抠图切割图 CUTOUT】 (图2：白底/透明底版)

| 序号 | 核验项目 | 判定结果 | 详细说明 |
| :--- | :--- | :--- | :--- |
| **1** | **全身完整无裁剪** | ✅ **通过** | 主体完整，无裁剪。 |
| **2** | **背景是否绝对平面纯黑/透明** | ⚠️ **存疑/不通过** | 图中背景显示为**白色**。若这是透明背景(PNG)在白色画布上的预览，则背景合格；若这是白底图，则不合格。CUTOUT要求背景全透明。 |
| **3** | **脚掌贴近画面底缘** | ✅ **通过** | 脚掌位置与RAW一致，贴近底缘。 |
| **4**
reasoning_content: 用户希望我作为资深游戏美术质检员，对提供的两张图片（原始立绘 RAW 和 抠图切割图 CUTOUT）进行逐项核验。

**设定分析：**
角色：腐坏人类丧尸 (zombie)
特征：破烂染血衣物、灰白腐皮、伤口可见、直立踉跄行走姿态、恐怖但可辨识。
限制：非守卫非主角。

**前置像素线索：** 底部角落纯净纯黑。
需要核实：全身完整、脚掌贴底、无白描边、双手手指分离、下半身明亮不融黑、轮廓平滑抗锯齿。

**图片分析：**
图1（RAW）：黑色背景，角色周围有一圈明显的白色描边（白边）。角色是一个丧尸，穿着破烂衣服，有血迹。
图2（CUTOUT）：白色背景（看起来像是透明背景被渲染成了白色，或者就是白色背景），角色周围依然有白色描边/轮廓线。仔细看，图2的背景是白色的，角色周围有一圈细细的黑线/灰线，看起来像是抠图没抠干净，或者保留了边缘的抗锯齿层。等等，仔细看图2，背景是白色的，角色周围有一圈细细的轮廓线。这看起来像是抠图后的结果，但是背景不是透明的，而是白色的。或者这是透明背景显示在白色画布上？
让我们重新仔细看图。
图1：背景是黑色的。角色周围有一圈很粗的白色描边。这明显是RAW图，但是带有明显的白色轮廓（可能是AI生成时的自动抠图边缘，或者为了突出主体加的白边）。
图2：背景是白色的。角色周围有一圈细细的深色轮廓。这看起来像是把图1的白色描边去掉了，或者反色了？不，图2看起来像是把图1的白色描边去掉了，但是留下了边缘的抗锯齿或者抠图痕迹。
等等，让我们仔细看用户的描述：“底部角落纯净纯黑”。
图1的底部角落确实是纯黑。
图2的背景是白色的。这通常意味着透明背景（Alpha channel）在白色背景下显示。

**逐项核验（针对 RAW - 图1）：**
1.  **全身完整无裁剪**：头顶完整，双手完整（手指可见），脚掌完整（虽然有点贴近底边，但看起来完整）。判定：通过。
2.  **背景是否绝对平面纯黑**：背景看起来是纯黑的。但是角色周围有一圈巨大的白色描边。这算背景的一部分吗？通常RAW图如果是黑底，背景应该是纯黑。这里有白边，说明背景不是绝对纯净的，或者主体边缘有问题。判定：不通过（有白边干扰，且白边很大）。
3.  **脚掌贴近画面底缘**：脚掌贴近底缘，下方留白很小。判定：通过。
4.  **轮廓上是否有白色描边/白边残留**：有非常明显的粗白色描边。判定：不通过。这是严重的缺陷。
5.  **双手可见且手指清晰分离**：左手（画面左侧）手指分离，右手（画面右侧）手指分离。判定：通过。
6.  **下半身是否明亮清晰**：下半身是灰蓝色的，比较明亮，没有融进黑背景（除了白边）。判定：通过（除了白边问题）。
7.  **Cutout相关**：这是RAW图，不适用。

**逐项核验（针对 CUTOUT - 图2）：**
图2看起来背景是白色的。通常CUTOUT应该是透明背景（PNG）。如果显示为白色，可能是预览问题。假设背景是透明的（Alpha）。
1.  **全身完整无裁剪**：同图1，完整。判定：通过。
2.  **背景是否绝对平面纯黑**：CUTOUT背景应该是透明。这里显示为白色，可能是透明背景。如果是透明背景，那就不存在“纯黑”的问题。但用户说“前置像素线索:底部角落纯净纯黑”。这说明RAW图底部是黑的。CUTOUT图底部看起来是白的/透明的。
    *   修正理解：用户给的图2背景是白色的。这通常表示透明通道（Alpha）被填充为白色，或者就是白底。如果是白底抠图，那就不符合“背景全透明”的要求。
    *   让我们假设图2是透明背景PNG，但在白色画布上显示。
3.  **脚掌贴近画面底缘**：同图1。判定：通过。
4.  **轮廓上是否有白色描边/白边残留**：图2中，角色边缘有一圈细细的深色/灰色线，这是原来的白边被抠图算法处理后的残留？或者是反色？
    *   仔细看图1，白边很粗。
    *   看图2，白边消失了，变成了细细的轮廓线。这说明抠图把白边去掉了？或者图2其实是把图1的白边保留了一部分？
    *   不，看图2的边缘，有一圈细细的灰黑色线。这看起来像是抠图没抠干净，保留了白边的边缘像素，或者抗锯齿层。
    *   实际上，图2看起来像是把图1的白色描边去掉了，但是留下了很细的轮廓。
    *   等等，让我们重新看用户的描述：“无白描边”。
    *   图1有明显的粗白边。 -> 不合格。
    *   图2（CUTOUT）：背景看起来是白色的。如果这是透明背景，那没问题。但是主体周围有一圈细细的轮廓。
    *   让我们仔细看CUTOUT的第7点要求：“背景全透明、主体完整无镂空大洞、无白边/黑边残留、无碎点杂质、轮廓干净平滑”。
    *   图2中，主体周围有一圈细细的线（看起来像深灰色或黑色细线），这属于“黑边残留”或“白边残留”（如果是反色的话）。
    *   而且，图2的背景如果是白色的，那就不是“全透明”。如果图2是透明背景显示在白色画布上，那我们需要检查边缘。
    *   边缘有一圈细细的线，这通常是抗锯齿或者抠图不干净。

**重新审视图片内容：**
图1（RAW）：黑色背景。角色周围有一圈**非常明显的白色粗描边**。这显然是AI生成时生成的“贴纸”效果，或者自动抠图加上的白边。
图2（CUTOUT）：白色背景（看起来像透明）。角色周围有一圈**细细的深色/灰色描边**。这说明抠图过程把白色描边去掉了，但是留下了边缘的过渡像素，或者这是一个错误的抠图，把白边变成了黑边/灰边。

**逐项判定逻辑：**

**【原始立绘 RAW】 (图1)**
1.  **全身完整**：头顶、双手（手指）、脚掌都在画面内。 -> **通过**。
2.  **背景纯黑**：背景大部分是纯黑，但角色周围有巨大的白色光晕/描边。 -> **不通过**（有白边干扰，且白边不属于背景颜色，属于主体错误）。
3.  **脚掌贴底**：脚掌贴近底边。 -> **通过**。
4.  **白色描边**：**有非常明显的粗白色描边**。 -> **不通过**。这是严重缺陷（旧档曾有锯齿硬切，这个有白边）。
5.  **双手手指**：手指分离清晰。 -> **通过**。
6.  **下半身明亮**：下半身清晰，不融黑（因为有白边挡着）。 -> **通过**。
7.  **Cutout判定**：不适用。

**【抠图切割图 CUTOUT】 (图2)**
*注意：图2背景显示为白色，通常代表透明背景（Alpha=255）在白色预览下，或者就是白底。如果是白底，那就不符合“背景全透明”。假设它是透明背景PNG。*
1.  **全身完整**：同RAW。 -> **通过**。
2.  **背景透明/纯黑**：如果是透明背景，则通过。如果是白底，则不通过。根据通常CUTOUT定义，应该是透明。 -> **假设通过**（如果是透明）。
3.  **脚掌贴底**：同RAW。 -> **通过**。
4.  **白描边/残留**：图2中，主体边缘有一圈细细的深色/灰色轮廓（看起来像是原白边的边缘残留，或者是抠图算法留下的黑边）。仔细看，这圈线是深灰色的。这属于“黑边残留”或“轮廓不干净”。 -> **不通过**（有残留边缘）。
5.  **双手手指**：手指分离。 -> **通过**。
6.  **下半身明亮**：下半身清晰。 -> **通过**。
7.  **Cutout质量**：
    *   背景全透明：看起来像（白底显示）。
    *   无白边/黑边残留：有细细的深色轮廓线残留。 -> **不通过**。
    *   轮廓干净平滑：因为那圈残留线，轮廓不干净。 -> **不通过**。

**综合判断：**
这张图最大的问题是**RAW图带有巨大的白色描边**。
通常AI生成角色立绘（如Midjourney或Stable Diffusion）如果提示词没有指定“no border”或者“transparent background”，很容易生成这种带白边的图。
用户的要求是“无白描边”。
RAW图有白描边 -> **需重生成** 或 **手动去除白边**。
CUTOUT图：看起来像是把白边去掉了，但是抠图抠得很薄，留下了边缘的杂色（那圈细细的线）。或者，CUTOUT图其实是把白边保留了一部分？
不，看图2，白边没了，变成了细细的轮廓。这说明抠图把白色部分去掉了（变成了透明），但是留下了边缘的抗锯齿像素（那些灰/黑色的细线）。
这就导致CUTOUT边缘不干净。

**修正建议：**
1.  RAW图必须重新生成，提示词加上 `no white border`, `clean edges`, `solid black background`。或者在PS里手动把白边去掉，把背景补黑。
2.  CUTOUT图需要重新抠图，或者使用“去除白边/黑边”工具，把边缘那圈细细的线修掉，确保边缘平滑且透明。

**详细逐项填写：**

**【原始立绘 RAW】**
1.  全身完整：是。头顶、手、脚都在。
2.  背景纯黑：否。有巨大白边。
3.  脚掌贴底：是。
4.  白色描边：**有，非常明显的粗白边**。
5.  双手手指：是，分离清晰。
6.  下半身：是，明亮。
7.  (N/A)

**【抠图切割图 CUTOUT】**
1.  全身完整：是。
2.  背景透明（假设白底代表透明）：是（视觉上）。
3.  脚掌贴底：是。
4.  白描边残留：**有，边缘有细细的深色/灰色轮廓线（原白边的残留/抠图边缘）**。
5.  双手手指：是。
6.  下半身：是。
7.  Cutout质量：**否**。有边缘残留（那圈细线），轮廓不干净平滑。

**最终判定：**
RAW：需重生成（因为白边太严重，且背景不纯）。
CUTOUT：需重生成/重抠图（因为边缘有残留线条，不干净）。

**修正建议：**
RAW图需要去除白色描边，或者重新生成（Prompt: solid black background, no border, clean edges）。
CUTOUT图需要重新抠图，使用“提取边缘”或“调整边缘”功能，彻底去除那圈残留的轮廓线，确保Alpha通道干净。

**再次仔细观察图片细节：**
图1（RAW）：白边非常粗，大概2-3像素宽，甚至更宽。这绝对不是抗锯齿，这是生成的边界。
图2（CUTOUT）：背景是白色的。这说明这是一个白底图，或者透明图显示在白色背景上。
如果是透明图显示在白色背景上：
- 角色边缘有一圈细细的灰黑色线。这说明抠图时，把白色的背景（图1的白边）抠掉了，但是留下了白边内侧的抗锯齿像素（那些灰/黑色的线）。
- 或者，抠图把白边保留了？不，白边不见了。
- 结论：CUTOUT图是图1去掉了白色主体后的结果？不对。
- 结论：CUTOUT图是图1把白色描边变成透明了。
- 问题：图2中角色周围有一圈细细的黑/灰线。这是“黑边残留”或“白边残留”（反色）。这不符合“无白边/黑边残留”。

**总结：**
两张图都有问题。
RAW图：有粗白边，背景不纯。 -> 不合格。
CUTOUT图：边缘有残留细线（黑/灰边），背景如果是白色则不合格（应为透明），如果是透明则边缘不干净。 -> 不合格。

**最终输出结构：**
1. RAW判定表。
2. CUTOUT判定表。
3. 最终判定与建议。

**针对第7点CUTOUT的具体描述：**
- 背景全透明：图2显示为白色，若为透明背景预览则算通过，若为白底则不通过。通常CUTOUT指Alpha通道。假设是透明。
- 主体完整：是。
- 无白边/黑边残留：**否**。图2边缘有一圈明显的深色细线（约1px），这是抠图不干净留下的原白边边缘或抗锯齿层。
- 无碎点：看起来还好。
- 轮廓干净平滑：**否**。因为那圈残留线。

**关于“下半身明亮不融黑”：**
在RAW图中，下半身（腿）是灰蓝色的，对比黑色背景是明亮的，没有融进去。
在CUTOUT图中，下半身也是明亮的。

**关于“旧档曾有锯齿硬切缺陷”：**
图1的白边非常硬，像硬切。
图2的边缘虽然细，但也比较硬，不够平滑抗锯齿。

**最终结论：** 两张图都需要修正。RAW必须重生成（去掉白边），CUTOUT必须重抠图（去边缘残留）。

**修正建议具体化：**
- RAW：Prompt增加 `solid black background`, `no outline`, `no border`, `transparent background` (如果是要直接出透明)。或者在PS里用魔棒选白色，删除，然后背景填黑。
- CUTOUT：用PS的“选择并遮住”或“调整边缘”，半径设大一点，把那圈残留线去掉。或者重新生成CUTOUT。

好了，逻辑理顺了。开始写回复。
---usage: {"completion_tokens": 4000, "prompt_tokens": 1977, "total_tokens": 5977, "completion_tokens_details": {"reasoning_tokens": 3296, "text_tokens": 704}, "prompt_tokens_details": {"cached_tokens": 0, "text_tokens": 437, "image_tokens": 1540}}
